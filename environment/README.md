This documentation will guide you through the several steps to build an
`environment` capable to consume the `s3gw` container.

# Prerequisite
Make sure you've installed the following applications:

- vagrant
- libvirt
